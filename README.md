# Simple-Text-Finder
That tiny utility can search any text pattern in files in choosed directory.
# How to use
You can choose directory to search and file extension. Enter a string pattern and application finds all matchings with this pattern in all files with selected extension in selected directory. You can navigate through file tree and open files right in application.
